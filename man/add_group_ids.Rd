% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_id.R
\name{add_group_ids}
\alias{add_group_ids}
\title{Data.table integration for group_id}
\usage{
add_group_ids(dt, cols, group_col = "group_id", use_regex = TRUE, ...)
}
\arguments{
\item{dt}{A data.table}

\item{cols}{Character vector of column names or regex patterns to use for grouping}

\item{group_col}{Name of the new column to create (default: "group_id")}

\item{use_regex}{Logical. Whether to treat 'cols' as regex patterns. Default TRUE.}

\item{...}{Additional arguments passed to group_id()}
}
\value{
The data.table with the new group_id column added
}
\description{
Adds group IDs as a new column to a data.table
}
\examples{
if (requireNamespace("data.table", quietly = TRUE)) {
  library(data.table)
  
  dt <- data.table(
    phone1 = c("123-456-7890", "987-654-3210", "123-456-7890", "", "555-0123"),
    phone2 = c("", "987-654-3210", "555-1234", "123-456-7890", ""),
    email = c("john@email.com", "jane@email.com", "bob@email.com", "john@email.com", "alice@email.com")
  )
  
  # Add group IDs based on phone columns using regex
  add_group_ids(dt, cols = "phone", incomparables = c(""))
  print(dt)
  
  # Add group IDs based on exact column names
  add_group_ids(dt, cols = c("phone1", "phone2"), use_regex = FALSE, incomparables = c(""))
  print(dt)
}

}
