% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_id.R
\name{group_id}
\alias{group_id}
\title{Multi-Column Group ID Assignment}
\usage{
group_id(
  data,
  cols = NULL,
  use_regex = TRUE,
  incomparables = c("", "NA", "Unknown"),
  case_sensitive = TRUE,
  min_group_size = 1,
  return_details = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data.frame or list of columns to group by}

\item{cols}{Character vector of column names or regex patterns to use for grouping (if data is data.frame)}

\item{use_regex}{Logical. Whether to treat 'cols' as regex patterns. Default TRUE.}

\item{incomparables}{Character vector of values to exclude from grouping (e.g., "", NA, "Unknown")}

\item{case_sensitive}{Logical. Whether string comparisons should be case sensitive. Default TRUE.}

\item{min_group_size}{Integer. Minimum group size to assign group ID (smaller groups get ID 0). Default 1.}

\item{return_details}{Logical. Whether to return detailed results including value mappings. Default FALSE.}

\item{verbose}{Logical. Whether to print timing and progress information. Default FALSE.}
}
\value{
If return_details=FALSE: Integer vector of group IDs for each row
        If return_details=TRUE: List containing:
        \item{group_ids}{Integer vector of group IDs for each row}
        \item{n_groups}{Total number of groups found}
        \item{group_sizes}{Integer vector of group sizes}
        \item{value_map}{List showing which values belong to which groups}
}
\description{
High-performance grouping based on shared values across multiple columns.
Uses Union-Find with path compression for optimal performance.
Perfect for entity resolution, deduplication, and finding connected records.
}
\examples{
# Basic usage with data.frame using exact column names
require(data.table)
df <- data.table(
  phone1 = c("123-456-7890", "987-654-3210", "123-456-7890", "", "555-0123"),
  phone2 = c("", "987-654-3210", "555-1234", "123-456-7890", ""),
  email = c("john@email.com", "jane@email.com", "bob@email.com",
            "john@email.com", "alice@email.com"),
  stringsAsFactors = FALSE
)

# Group by phone columns using regex (default)
group_ids <- group_id(df, cols = "phone", incomparables = c(""))
print(group_ids)

# Within a data.table
# Group by phone columns using regex (default)
dt2 <- copy(df)
dt2[, gid := group_id(dt2, cols = "phone", incomparables = c(""))]
print(dt2)

# Group by phone columns using exact names
group_ids <- group_id(df, cols = c("phone1", "phone2"), use_regex = FALSE, incomparables = c(""))
print(group_ids)

# Group by all columns matching pattern with detailed output
result <- group_id(df, cols = "phone|email", 
                   incomparables = c("", "NA"), 
                   return_details = TRUE)
print(result)

# Direct list input
phone1 <- c("123-456-7890", "987-654-3210", "123-456-7890", "", "555-0123")
phone2 <- c("", "987-654-3210", "555-1234", "123-456-7890", "")
email <- c("john@email.com", "jane@email.com", "bob@email.com",
           "john@email.com", "alice@email.com")

group_id(list(phone1, phone2, email), incomparables = c(""))

}
