% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_edge_components.R
\name{get_edge_components}
\alias{get_edge_components}
\title{Get Edge Component Assignments}
\usage{
get_edge_components(
  edges,
  n_nodes = NULL,
  compress = TRUE,
  return_type = "list"
)
}
\arguments{
\item{edges}{A two-column matrix or data.frame where each row represents an edge}

\item{n_nodes}{Optional. Total number of nodes. If not provided, inferred from edges.}

\item{compress}{Logical. Whether to compress component IDs. Default is TRUE.}

\item{return_type}{Character. Either "list" (default) for separate from/to vectors,
or "combined" for a single vector of from components only.}
}
\value{
If return_type="list": List with from_components, to_components, n_components.
  If return_type="combined": Integer vector of from_components (same length as input edges).
}
\description{
Efficiently returns component assignments for each edge in the input.
This is much faster than computing connected components separately and 
then doing lookups in R.
}
\examples{
edges <- matrix(c(1,2, 2,3, 5,6), ncol=2, byrow=TRUE)
get_edge_components(edges)

# For your specific use case:
get_edge_components(edges, return_type = "combined")

}
